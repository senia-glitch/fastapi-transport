# fastbase: generated
"""Точка входа приложения.

Всё поведение настраивается через .env.fastbase (префикс FAT_).
"""

from fastbase import make_app, start

app = make_app()


if __name__ == "__main__":
    start()