# fastbase: generated
[project]
name = "app"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastbase @ git+https://github.com/senia-glitch/fastapi-transport.git",
]

[project.scripts]
app = "app.main:main"